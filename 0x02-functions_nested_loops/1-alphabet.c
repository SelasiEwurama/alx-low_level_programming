#include "main.h"

/**
 * print alphabet - function to print alphabets in lower case
 * return 0;
 */

void print_alphabet(void);
{
	char i;

	for (i = 'a' ; i <= 'z' i++)
		_putchar(i);
	_putchar('\n');
}
